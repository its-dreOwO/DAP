notes_DAP/
app/.venv/
__pycache__/
*.pyc
.~lock.*
*.log

# RandomForest is now bounded (max_depth=20, min_samples_leaf=20) → ~2.3MB, tracked.

# Raw DataCo dataset (large; tokenized_access_logs.csv is unused). Re-download via:
#   kaggle datasets download -d shashwatwork/dataco-smart-supply-chain-for-big-data-analysis -p Data/dataco_raw --unzip
Data/dataco_raw/

# Cleaned DataCo CSV is 52MB (180k rows). Regenerate via src-code/01_ingestion_cleaning.py
Data/filtered/clean_data.csv

# Streamlit secrets (never commit API keys)
.streamlit/secrets.toml

# Codegraph MCP index/daemon (machine-local, never commit)
.codegraph/
